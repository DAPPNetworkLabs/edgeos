
export const networkextensions = {
    // websocket
    "network_fetch":async({json, pid, cb, edgeOSKernel})=>{
    },
    "network_websocket":async({json, pid, cb, edgeOSKernel})=>{
    },
    // webrequests
}