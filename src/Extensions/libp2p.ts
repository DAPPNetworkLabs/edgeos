
export const libp2pextensions = {
    "libp2p_subscribe":async({json, pid, cb, edgeOSKernel})=>{
    },
    "libp2p_publish":async({json, pid, cb, edgeOSKernel})=>{
    },
    "libp2p_send":async({json, pid, cb, edgeOSKernel})=>{
    },
    "libp2p_onmessage":async({json, pid, cb, edgeOSKernel})=>{
    },
    // pubsub
    // p2p
    // discovery
}